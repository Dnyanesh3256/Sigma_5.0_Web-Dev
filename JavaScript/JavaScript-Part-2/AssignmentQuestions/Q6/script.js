let numOne = 32;
let numTwo = 47852;

if((numOne%10) === (numTwo%10)){
    console.log(`${numOne} and ${numTwo} have same last digit.`);
}else{
    console.log(`${numOne} and ${numTwo} doesn't have same last digit.`);
}